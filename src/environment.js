// Central hybrid environment boundary
// Non-destructive flag surface for AR + Warehouse coexistence.
export const ENV = {
  mode: "hybrid", // other possible future values: "ar-only", "warehouse-only"
  AR: true,
  Warehouse: true,
  version: "bootstrap-1"
};
