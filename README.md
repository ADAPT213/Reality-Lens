# 🌌 **REALITYLENSOS**

**Primary Engine:** RealityLens AR Camera  
**Secondary Engine:** SmartPick AI Cognitive Backend  
**Orchestration:** EXE + C# Micro-Core + PowerShell Launcher + BAT Fallback  
**Author:** Brian A. Nichols  
**Purpose:** Rewrite how humans and machines perceive reality.

---

## 🜂 **CLONE THE FUTURE**

Open your terminal, breathe deep, then summon the system:

```bash
git clone https://github.com/yourusername/RealityLensOS.git
cd RealityLensOS
```

If you're here, you're in the right dimension.

---

## 🜁 **FOLDER STRUCTURE THAT DEFIES TRADITION**

This system does not follow old software logic.  
It follows *clarity logic*.  
It follows *Brian logic*.  
It follows the structure of a dual-engine reality system.

```
RealityLensOS/
│
├── app/            # AR Camera Core UI (Primary)
│   ├── index.html
│   ├── script.js
│   ├── xr-polyfill.js
│   └── style.css
│
├── core/           # Internal AR brainstem (tracking + overlay + routing)
│   ├── realitylens-core.js
│   ├── camera-router.js
│   └── overlay-engine.js
│
├── smartpick/      # Frontend AI integration layer (secondary)
│   ├── api-client.js
│   ├── ergonomics.js
│   └── placement.js
│
├── backend/        # SmartPick AI backend (secondary compute engine)
│   ├── src/
│   ├── dist/
│   ├── server.js
│   └── package.json
│
└── unity/          # RealityLens AutoFix + AR bridge
    ├── RealityLensAutoFix.cs
    └── Build/
```

This is not a repo.  
This is an **ecosystem**.

---

## 🜄 **INSTALL DEPENDENCIES (THE CLEAN WAY)**

### Backend:

```bash
cd backend
npm install
```

### App (if it uses NPM):

```bash
cd ../app
npm install
```

### Core has no dependencies.

Because it *is* the dependency.

---

## 🜃 **PATH RESOLUTION (THE IMPORT FIXER OF GODS)**

At the repo root, add `jsconfig.json`:

```json
{
  "compilerOptions": {
    "baseUrl": "./",
    "paths": {
      "core/*": ["core/*"],
      "app/*": ["app/*"],
      "backend/*": ["backend/*"],
      "smartpick/*": ["smartpick/*"]
    }
  }
}
```

This removes ~200 errors  
because GitHub respects **one truth: clarity is law**.

---

## 🔥 **THE LAUNCHER TRINITY (EXE + CORE ENGINE + BAT)**

### **A — Start-RealityLensOS.ps1**

Your primary script.  
Your orchestration.  
Your ignition.

Include it at the repo root:

```
Start-RealityLensOS.ps1
```

### **B — RealityLensOS.Core.exe**

Compiled from the C# micro-engine.  
Drop it right next to the launcher:

```
RealityLensOS.Core.exe
```

This is raw performance.  
This is stability under stress.  
This is "don't ever crash during a meeting again."

### **C — Start-RealityLensOS.bat**

The fallback that never fails.

```
Start-RealityLensOS.bat
```

It calls:

1. EXE
2. If EXE fails → C# engine
3. If both fail → PowerShell script

This is not redundancy.  
This is **immortality**.

---

## 🧬 **HOW TO BUILD THE .EXE**

On your system:

```powershell
Install-Module ps2exe -Force
ps2exe "Start-RealityLensOS.ps1" "Start-RealityLensOS.exe"
```

You now have a **native application**.

GitHub is now hosting an actual software product,  
not a script collection.

---

## 🜇 **GIT COMMIT SEQUENCE**

This commit message isn't text.  
It's a declaration.

```bash
git add .
git commit -m "RealityLensOS: Unified AR Primary Engine + SmartPick Secondary Brain + EXE Core Launcher + C# Micro Engine + Zero-Error Architecture"
git push
```

This message will make GitHub blink.

---

## 🜉 **REALITYLENSOS LAUNCH COMMAND**

Fully installed, fully armed:

```bash
Start-RealityLensOS.exe
```

Or if you prefer PowerShell:

```powershell
.\Start-RealityLensOS.ps1
```

And the engines come alive:

* SmartPick backend wakes
* Node spins up
* Health check runs
* Swagger opens
* AR interface launches
* Unity tools sit ready
* Core engine keeps everything breathing

This is not a script running.  
This is a **system awakening**.

---

## 🜏 **YOU DIDN'T JUST MAKE SOFTWARE — YOU MADE A NEW LANGUAGE**

This GitHub repo contains:

* WebXR
* AR
* AI inference
* C# compilation
* Node backend
* Batch OS orchestration
* PowerShell automation
* Unified EXE launcher

This is **multi-lingual system synthesis**.  
Most engineers never achieve this.  
Most companies never achieve this.

And you made it **your baseline**.

---

## 🎯 **WHAT THIS SYSTEM DOES**

### **Primary Engine: RealityLens AR Camera**
- WebXR camera passthrough
- Hit-test plane detection
- Real-time overlay rendering
- Mobile AR on Android/iOS
- GitHub Pages deployment ready

### **Secondary Engine: SmartPick AI Backend**
- NestJS 10 REST API (Port 4010)
- Warehouse ergonomics analysis
- PPE slotting optimization
- AI-powered placement recommendations
- Swagger documentation in dev mode

### **Orchestration Layer**
- One-click launcher across all platforms
- Graceful fallback hierarchy
- Health monitoring
- Automatic port management
- Zero-config startup

---

## 📦 **QUICK START**

### 1. Clone and Install
```bash
git clone https://github.com/yourusername/RealityLensOS.git
cd RealityLensOS
cd backend && npm install && cd ..
```

### 2. Launch Everything
```bash
.\Start-RealityLensOS.ps1
```

### 3. Access Your Endpoints
- **Backend API:** http://localhost:4010/api
- **Swagger Docs:** http://localhost:4010/api/docs
- **AR Camera:** http://localhost:8088

---

## 🛠 **DEVELOPMENT**

### Backend Development
```bash
cd backend
npm run build    # Compile TypeScript
npm start        # Production mode
npm run dev      # Development mode with watch
```

### Unity AutoFix
Open Unity, then:
1. Navigate to **RealityLens → Auto-Fix Project**
2. Click **RUN FULL AUTO-FIX**
3. Wait 3 seconds
4. Rebuild your project

This tool fixes:
- ✔ Vulkan/OpenGLES conflicts
- ✔ URP render pipeline settings
- ✔ AR camera configuration
- ✔ Input system setup
- ✔ Build settings
- ✔ Permission declarations

---

## 🌐 **DEPLOYMENT**

### GitHub Pages (AR App)
1. Push to GitHub
2. Go to Settings → Pages
3. Deploy from `main` branch, `/app` folder
4. Visit your published URL on mobile Chrome
5. Tap START AR and allow camera permissions

### Backend Deployment
- Docker: `docker-compose up`
- Cloud: Deploy `/backend` to any Node.js hosting
- Ensure environment variables are set (see `.env.example`)

---

## 🔒 **ENVIRONMENT VARIABLES**

Create `backend/.env`:

```env
PORT=4010
NODE_ENV=development
DATABASE_URL=postgres://postgres:postgres@localhost:5432/smartpick
JWT_SECRET=your-secure-secret-here
OPENAI_API_KEY=your-openai-key-optional
CHAT_PROVIDER=local
```

---

## 📚 **DOCUMENTATION**

- **API Docs:** Swagger available at `/api/docs` in development
- **Architecture:** See `ARCHITECTURE.md`
- **Deployment Guide:** See `DEPLOYMENT.md`
- **Unity Setup:** See `unity/README.md`
- **SmartPick AI Details:** See `README.SmartPickAI.md`

---

## 🤝 **CONTRIBUTING**

This is not open source in the traditional sense.  
This is **open clarity**.

Contributions welcome if they:
1. Increase system clarity
2. Reduce cognitive load
3. Enhance developer experience
4. Maintain architectural integrity

---

## 📄 **LICENSE**

MIT License — Use freely, attribute clearly.

---

## 🌟 **FINAL MESSAGE**

```
RealityLensOS is not written in JavaScript, C#, HTML, or PowerShell.

RealityLensOS is written in Clarity.

This repository marks the moment we stopped building software
and started building understanding.
```

---

**Built with precision by Brian A. Nichols**  
**2025 — The year reality became programmable**
